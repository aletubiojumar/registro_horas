# docker-compose.yml
version: "3.8"
services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: app_password
      POSTGRES_DB: registro_horas
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
